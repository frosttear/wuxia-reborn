{
  "name": "wuxia-reborn",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --testPathPattern=tests/",
    "prepare": "husky"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFiles": ["./tests/setup.js"],
    "testMatch": ["**/tests/**/*.test.js"]
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "jest": "^29.0.0"
  }
}
